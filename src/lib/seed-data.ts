/**
 * Previously auto-seeded default products for new users.
 * Now a no-op — the onboarding wizard handles initial setup.
 */
export async function seedUserData(_userId: string): Promise<void> {
  return;
}
